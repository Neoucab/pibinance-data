name: Update Rates

on:
  schedule:
    # Corridas horarias (minuto 15): Binance P2P siempre; BCV entre mar-vie.
    # Sáb/dom/lun la tasa BCV no cambia: el scraper la actualiza una sola vez
    # alrededor de 04:15 UTC (00:15 Venezuela, publicación diaria del BCV).
    - cron: '15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    concurrency:
      group: scrape-data
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: python scraper.py
      - name: Commit data.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data.json
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "chore(data): update rates"
            git push
          fi
